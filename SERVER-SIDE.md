# Note - Server side needed Features
* push to server via api
    * epubs (not updating but for saving purposes)
    * **put** name, url, author and other metadata via cli -> save in database after downloading serverside
        * updating and saving update status epub that is saved within database
    * **get - downloading** epub and other metadata (when last updated)
    + **get - other client features**
        * list of all epubs and metadata based on given json config