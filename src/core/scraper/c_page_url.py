from urllib.parse import urlparse

from src.core.url_builder import URLBuilder

# generate chapter page url (without requesting)
class ChapterPageURL:
    def __init__(self, url: str, ):
        self.__url_builder: URLBuilder = URLBuilder(url)

    def get_id(self)
        
    def get_chapter_page_url() -> str:
