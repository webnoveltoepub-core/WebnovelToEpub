from src.core.http_request.flaresolverr_client import FlaresolverrClient

from src.core.config.verify_json import ConfigVerify
from src.core.config.config_handler import ConfigHandler

from src.core.scraper.html_scraper import Scraper

# simplifies main function; scraping, building and modifing epubs
class Core:
    def __init__(self):
        verify: Con = 

    # returns a bytestream epub; url: to webnovel side
    def get_webnovel(url: str):
        Scraper.
    def get_chapter_urls(self) -> list[str] | None:
    def get_content(self, chapter_urls: list[str]) -> :
